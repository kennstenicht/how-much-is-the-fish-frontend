{{#each @model as |event|}}
  <LinkTo
    @route="workspaces.workspace.events.event"
    @models={{array this.workspace event}}
  >
    {{moment-format event.date "YYYY-MM-DD"}} - {{event.name}}
  </LinkTo><br>
{{/each}}
